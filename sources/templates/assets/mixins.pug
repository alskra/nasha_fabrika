include ../../../node_modules/bempug/index

mixin resetCase(str)
  = str[0].toUpperCase() + str.slice(1).toLowerCase()

mixin a(href)
  a(href= href || jsHref)&attributes(attributes)
    block

mixin aMail(mail)
  a(href='mailto:' + mail)&attributes(attributes)
    block

mixin aTel(tel)
  a(href='tel:' + tel)&attributes(attributes)
    block

mixin time()
  time(datetime='ГГГГ-ММ-ДДTчч:мм:сс±чч:мм')&attributes(attributes)
    block